# 2026-06-17 工作日志

## GitHub Pages 固定部署配置
- 用户注册 GitHub 账号：lianjun5260
- 创建仓库：lianjun5260/dabai-word
- 生成 Personal Access Token (repo scope)
- SSH 密钥加入 GitHub 账户
- git push 代码到 GitHub main 分支
- 配置 Pages：source=main, path=/docs
- 踩坑：初次构建 errored，原因是缺少 .nojekyll 文件 → 添加后构建成功
- 永久地址：https://lianjun5260.github.io/dabai-word/
- 后续工作流：改 prototype/ → 同步到 docs/ → git push → 手机刷新即更新
- 试过 Gitee Pages，需要实名认证，放弃

## P23 返回按钮规范
- 用户要求左上角「返回首页」→ `<`（尖括号，不是 ← 箭头）
- 初次错误改成了 ←，用户指正后修正为 `<`
- **项目规范**：全部页面左上角返回统一用 `<`，class `.back`，功能 goBack() 或 goHome()
- 已写入 MEMORY.md 作为项目 UI 规范

## P23/P23a/P23b 词库三页面重构
- 全站 15 处返回按钮从 `←` 改为 `<`（用户强制规范）
- 创建 bankData 数据模型，解除硬编码
- P23 词库列表：`⋯` 调用 openBankDetail(name) 动态加载对应词库
- P23a 词库详情：改动态渲染，标题/概述/素材列表全部根据 currentBank 加载
- P23b 创建词库（新页面）：名称输入 + 图标选择(📕📗📘📙) + 创建后自动跳 P23a
- createBank() 改为进入 P23b；doCreateBank() 写入 bankData → openBankDetail()
- 重命名：toggleRename/saveRename 改写为操作 bankData 的键名
- 删除词库：deleteBank() 从 bankData 移除对应条目
- 素材管理：addMaterialToList/deleteMaterial 读写 bankData[currentBank].materials
- 生成词库：generateBank() 完成后更新 bankData 中的 total/mastered/learning 并刷新 UI
- SRS v2.2：新增 FR-DICT-005（P23b），修正 FR-DICT-003/004，页面索引表补充 P23b
