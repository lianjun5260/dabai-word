个枚举值：3.4 单词来源枚举（source 字段）批量导入数据自动去重，重复数据系统自动标记并跳过，不覆盖原有数据3.3 单词生命周期与状态通用规则单词词条在整个生命周期中包含以下核心状态流转（审核池词条与非审核态词条均存储于同一张单词主表，通过 status 状态位区分）：待上架（status=0）：审核通过已写入单词主表，但尚未发布到生产环境，前端不可见、不参与学习。管理员可在后台浏览、编辑黑名单（status=4）：管理员审核驳回或从单词管理移入黑名单的单词，不再出现在审核池和正常单词列表中。频次正常累计不受影响。可恢复至审核池（status=4→3）重新审核。黑名单单词被永久拦截：相同单词再次新增时跳过审核池，不显示在待审核列表中。已上架（status=1）：已发布到生产环境，前端正常展示、用户可学习已下架（status=2）：管理员主动下架，后台保留全部数据，前端隐藏、不展示、不参与任何学习任务。可随时重新上架恢复审核中（status=3）：AI识别生成的词条自动写入单词主表，状态标记为「审核中」，进入审核池列表。管理员审核通过后状态变更为「待上架」（status=0），审