# 2026-06-20 工作日志

## P32 LC-06 字母行DOM差量更新修复
- 问题：移动端Chrome上，字母下划线行显示顺序与键盘输入顺序相反（输入abc→显示cba）
- 根因：renderSpellLine() 每次 oninput 都用 innerHTML 重建整个flex容器，移动端Chrome flex渲染存在时序问题
- 修复：改为DOM差量更新
  - loadLc06() 中创建 cell DOM 节点并存入 lc06State._cells[]
  - renderSpellLine() 只更新每个 cell 的 textContent 和 className，不重建容器
- 不影响其他组件（renderSpellLine 仅 LC-06 使用）

## P32 LC-06 多次尝试修复顺序问题
- 尝试1：DOM差量更新 → 仍反
- 尝试2：input加dir=ltr + spell-line显式direction:ltr + fixed定位到屏幕外 → 仍反+退格失效
- 尝试3：input改回文档流(h:0+overflow) → 仍反
- 尝试4：beforeinput事件追踪，不读input.value → 仍反

## P32 LC-06 最终方案：自定义QWERTY键盘组件
- 彻底去掉手机系统键盘，改为内置HTML/CSS QWERTY键盘
- 3行字母键（QWERTY布局）+ ⌫ 删除键
- 点击写入lc06State.userInput，顺序完全可控
- 退格正常，无障碍
- CSS新增 .kbd-key / .kbd-del 样式
- JS新增 lc06TypeKey(key) 函数，简化为纯DOM事件
- ✅ 手机端验证通过
