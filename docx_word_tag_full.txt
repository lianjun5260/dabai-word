随机抽取，保持学习新鲜感；每条例句独立配发音，支持逐句听读。5.2 词库标签表（word_tag）词库标签以学段/考试类别为核心维度对单词进行归类。管理员可在后台对标签进行增删改查。标签与单词之间通过单词主表的 stage_label 字段关联（多值逗号分隔），同一单词可绑定多个标签。索引设计（1万条记录，常规查询）：• PRIMARY KEY (id)• INDEX idx_rf_tag (tag) — 按标签筛选索引设计（5000条记录，常规查询）：• PRIMARY KEY (id)• INDEX idx_rt_time (upload_time DESC) — 按时间排序• INDEX idx_rt_operator (operator) — 按操作人筛选• INDEX idx_rf_folder (folder) — 按文件夹筛选• INDEX idx_rf_status (status) — 按状态筛选（正常/失效）• INDEX idx_rf_upload_time (upload_time DESC) — 按上传时间排序• INDEX idx_rf_type (file_type) — 按文件类型筛选5.3 文件-单词频次溯源关联表（file_word_frequency）索引与分区设计（100万+条记录，系统最高频写入+查询表）：• PRIMARY KEY (id)• UNIQUE KEY uk_fwf_fwt (file_id, word_id, tag_id) — 防止同一文件重复记录同一单词+标签• INDEX idx_fwf_word (word_id) — 按单词查询溯源（「这个单词被哪些文件引用过」）• INDEX idx_fwf_file (file_id) — 按文件查询溯源（「这个文件贡献了哪些词的频次」）• INDEX idx_fwf_tag (tag_id) — 按标签查询溯源分区策略：HASH(file_id) PARTITIONS 8 — 按 file_id 哈希分为 8 个分区，每分区约 12~15 万条。写入均匀分散无热点，文件删除/失效时直接命中单个分区，分区裁剪精准高效。创建语句：ALTER TABLE file_word_frequency PARTITION BY HASH(file_id) PARTITIONS 8;用于精准绑定资源库中每一份上传文件与对应单词的频次增量，实现删文件精准扣减、数据对账、全链路溯源，为频次数据准确性提供底层支撑。字段名字段类型必填默认值可NULL说明idbigint是自增否主键自增file_idvarchar(64)是无否上传文件唯一IDfile_namevarchar(255)是无否文件名称word_idbigint是无否关联单词主表IDwordvarchar(50)是无否关联单词名称（冗余，方便查询）freq_incrementint是0否本次文件内该单词新增频次（单文件出现次数）tag_idbigint是无否关联标签ID，用于按标签精确扣减频次task_idvarchar(64)是无否识别任务ID，关联日志溯源upload_userbigint是无否上传操作人IDupload_timedatetime是当前时间否文件上传时间freq_statustinyin