ough_delete_timedatetime否无是彻底删除时间update_timedatetime否无是最近修改时间5.6 识别任务日志表（recognition_task）5.8 单词变形规则表（word_inflection_rule）存储系统识别的所有单词变形规则，是形态归集引擎的核心数据表。系统初始化时由大模型一次性生成约250条基础规则，后续运行时遇到新变形自动补充。字段设计：┌──────────────┬─────────────┬──────────────────────────────────┐│ 字段          │ 类型         │ 说明                              │├──────────────┼─────────────┼──────────────────────────────────┤索引设计（50万+条记录，高频写入+条件查询）：• PRIMARY KEY (id)• INDEX idx_ol_type (type) — 按操作类型筛选• INDEX idx_ol_operator (operator) — 按操作人筛选• INDEX idx_ol_time (operation_time DESC) — 按时间范围查询• INDEX idx_ol_target (target_type, target_id) — 按目标对象查询分区策略：RANGE(operation_time) 按月分区 — 每月约7000条，3年36个分区。优势：按日期查询时分区裁剪精准，历史分区可归档/压缩，DROP PARTITION 高效清理过期日志创建语句：CREATE INDEX idx_ol_time ON operation_log(operation_time DESC); CREATE INDEX idx_ol_type ON operation_log(type); ALTER TABLE operation_log PARTITION BY RANGE (TO_DAYS(operation_time)) (PARTITION p2026 VALUES LESS THAN (TO_DAYS("2027-01-01")), ...);│ id           │ bigint      │ 主键自增                           ││ inflected    │ varchar(100)│ 变形词（识别到的原始形态，如 went）   ││ lemma        │ varchar(100)│ 原形主词（归集目标，如 go）          ││ type         │ varchar(30) │ 变形类型（规则/不规则/异形等）      ││ priority     │ tinyint     │ 来源优先级：0=系统内置 1=AI自动 2=低置信│ create_time  │ datetime    │ 创建时间                           ││ update_time  │ datetime    │ 更新时间                           │└──────────────┴─────────────┴──────────────────────────────────┘唯一索引：UNIQUE(inflected, type)type 枚举值：regular（规则变形）/ irregular_past（不规则过去式/过去分词）/ irregular_plural（不规则复数）/ irregular_comp（不规则比较级）/ variant（异形词变体）/ base（原形）5.9 单词同源关联字段（word_relate）word 主表新增 word_relate 字段（JSON 类型），用于存储不规则变形词与主词之间的同源双向关联关系。存储格式示例：made 记录：{"lemma":"make","type":"irregular_past"}  // 表示 made 是 make 的不规则过去式make 记录：{"inflected":["made","making"],"type":"irregular_past"}  // 表示 make 的不规则变形包含 made前端使用：单词详情页根据此字段展示同源关联词列表，支持管理员手动修正。字段名字段类型必填默认值可NULL说明idbigint是自增否主键自增task_idvarchar(64)是无否任务唯一IDfile_namevarchar(500)是无否上传文件名（多文件逗号分隔）file_countint是1否本次任务上传文件数量file_typevarchar(50)是无否文件类型sourcetinyint是0否来源：0=单词库上传，1=资源库上传tag_idbigint否无是关联标签IDfolder_idbigint否无是关联文件夹ID（资源库来源时有效）total_countint是0否识别总条数duplicate_countint是0否重复条数error_countint是0否错误条数new_word_countint是0否新增单词数（单词主表此前不存在的词条数）passed_countint是0否审核通过条数（status=0）freq_increment_totalint是0否本次累计新增单词频次总量task_statustinyint是0否任务状态：0=处理中，1=已完成，2=失败，3=已取消error_msgtext否无是失败错误信息upload_userbigint是无否上传人IDcreate_timedatetime是当前时间否任务创建时间finish_timedatetime否无是任务完成时间5.7 操作日志表（operation_log）字段名字段类型必填默认值可NULL说明idbigint是自增否主键自增operator_idbigint是无否操作