# 2026-06-17 工作日志

## GitHub Pages 固定部署配置
- 用户注册 GitHub 账号：lianjun5260
- 创建仓库：lianjun5260/dabai-word
- 生成 Personal Access Token (repo scope)
- SSH 密钥加入 GitHub 账户
- git push 代码到 GitHub main 分支
- 配置 Pages：source=main, path=/docs
- 踩坑：初次构建 errored，原因是缺少 .nojekyll 文件 → 添加后构建成功
- 永久地址：https://lianjun5260.github.io/dabai-word/
- 后续工作流：改 prototype/ → 同步到 docs/ → git push → 手机刷新即更新
- 试过 Gitee Pages，需要实名认证，放弃

## P23 返回按钮规范
- 用户要求左上角「返回首页」→ `<`（尖括号，不是 ← 箭头）
- 初次错误改成了 ←，用户指正后修正为 `<`
- **项目规范**：全部页面左上角返回统一用 `<`，class `.back`，功能 goBack() 或 goHome()
- 已写入 MEMORY.md 作为项目 UI 规范

## P23b 废弃 → P23a 重构为词库详情统一模板
- 去掉 「+ 添加素材」按钮和「生成词库」按钮
- 底部固定：📷 拍照 / 📁 上传文件 两个按钮
- 素材列表含实时进度条 + 解析状态机
- 素材状态：parsing(解析中) / paused(已暂停) / completed(已完成) / failed(失败)
- 操作：解析中可暂停/删除，暂停后可继续/删除，失败后可重试/删除
- 解析完成自动统计词数并更新学习概览
- 删除确认弹窗：⚠️ 提示素材和单词数据无法恢复
- 空词库和有素材词库共用同一套 HTML 模板
- SRS FR-DICT-004 更新：素材管理交互规范、删除确认弹窗规范
- GitHub 推送策略优化：先用 HTTPS，失败再用 SSH + StrictHostKeyChecking=no

## P23a 素材卡片 v2：卡片自身即进度条
- 去掉细进度条行、删除按钮、「识别完成」文案
- 卡片背景渐变色从左到右铺满 = 进度
- 解析中：绿色渐变 + 右上「暂停」+ 右下百分比
- 暂停：黄色底 + 右上「继续」
- 完成：全绿底 → 名称后显示「识别出X词」
- 失败：红色底 + 右上「重试」
- 长按素材 0.8s 弹出 confirm 删除
- manifest.json version 自动更新为时间戳

## P26 我的收藏页
- 全新页面：两段式Tab「正在学习(N)/已掌握(N)」
- 12个模拟单词完整覆盖标签
- 左滑露出红色「取消收藏」按钮（touchstart/touchmove/touchend）
- 底部固定「📚 开始复习」按钮
- 空状态设计：引导用户去首页收藏
- 与P20单词详情页联动（★收藏切换）
- 复习时间格式化：formatReviewTime(dueDate)

## P20 单词详情页动态化
- openWordDetail(word) 从 wdDB 加载12个单词完整数据
- 对齐后台SRS单词表24个字段
- 动态渲染：词形变化/常用短语/多条例句/扩展信息
- 标签(考试类型)/难度/频次(总/教材/考试)/助记
- ★收藏切换联动 favData

## Bug修复：底部Tab状态同步 + goBack()函数补充 (2026-06-17 10:04)
- showPage() 新增 tabIdxMap {'P04':0,'P05a':1,'P21':2}，页面切换时同步所有 .tab-item.active
- goHome() 中同步底部Tab栏
- **严重Bug**: 12处返回按钮和1处手势代码调用 goBack() 但函数未定义 → 新增 goBack() 函数
- SRS 更新至 v2.5
- 推送到 GitHub Pages

## Bug修复：P21(我的)页面 id 属性缺失 (2026-06-17 15:48)
- **根因**: P20和P22之间「我的」页面缺少 `<div class="page" id="P21">` 开头标签
- `showPage('P21')` → `getElementById('P21')` 返回 null → 短路 return → 点击Tab无响应
- **修复**: 在P20关闭标签后插入 `<div class="page" id="P21">`，div嵌套平衡已验证
- 影响范围：所有底部Tab中的「我的」按钮、P22返回按钮、P26返回按钮均无效
- 已推送GitHub Pages，用户需刷新（加?v=06171548绕过CDN）

## 收藏页数据扩展 + 单词卡片交互 (2026-06-17 16:00)
- favData 从12条扩展到33条：正在学习15条 + 已掌握18条
- wdDB 新增21个单词的完整数据（音标/词性/短语/例句/词形/标签/难度/频次/助记）
- 卡片交互增强：CSS .fav-word-click 类
  - :active 状态：scale(.97) + 品牌绿背景(brand-light)反馈
  - 右侧 › 箭头指示可点击
  - 平滑 transition .12s
  - -webkit-tap-highlight-color:transparent 消除默认高亮
- 修复 renderFavorites 中重复 `list.innerHTML = ''` 
- 将 fav-row/fav-inner 内联样式提升为 CSS 类
- SRS 更新至 v2.6

## 卡片交互优化：整卡点击 + 整卡高亮 (2026-06-17 16:11)
- 将 onclick+openWordDetail 从 .fav-word-click（单词行）提升到 .fav-inner（整张卡片）
- :active 按压效果从单词行扩展到整卡：全卡背景变 brand-light 绿 + scale(.98)
- 右侧 › 箭头随整卡定位（.fav-inner::after），按压同步变绿
- 与左滑删除不冲突：touch事件在 .fav-row，click 在 .fav-inner，stopPropagation 隔离
- .fav-inner 新增 cursor:pointer / user-select:none / -webkit-tap-highlight-color:transparent

## 错题本功能 P27/P27a (2026-06-17 16:33)
- P27 错题本首页：24阶段×3分组卡片，渐变背景+错误数+最近错题时间，空态灰色不可点击
- P27a 阶段错题列表：完整复用 fav-inner/fav-row 卡片样式，整卡点击绿闪→详情、左滑移除
- 错误本数据 errorBookData：24阶段中12个有MOCK数据（一年级~雅思），共60+错题
- 新增 errTouchStart/Move/End 独立触摸处理，tapErrCard 绿闪反馈
- P21菜单「错题本」链接到P27，动态显示总错题数
- 导航面板新增 P26/P27/P27a 入口
