致，前端渲染复用同一套组件。5 数据库核心字段设计5.0 管理员用户表（admin_user）存储后台管理员的账号信息，用于登录鉴权。系统采用平权设计，所有登录用户拥有全部操作权限，无角色区分。字段名 | 类型 | 说明id | bigint | 主键，自增username | varchar(50) | 登录用户名，唯一，不可重复password_hash | varchar(255) | 密码哈希值（BCrypt 加密存储，禁止明文）last_login_time | datetime | 最近一次登录时间，首次登录前为 NULLis_force_change_pwd | tinyint | 是否强制修改密码（1=是，0=否），首次登录默认为 1，修改密码后变更为 0create_time | datetime | 账号创建时间is_protected | tinyint | 是否系统保护账号（1=是，不可删除，如 admin），默认为 0索引设计：UNIQUE KEY uk_username (username)创建语句：CREATE UNIQUE INDEX idx_admin_username ON admin_user(username);update_time | datetime | 最近一次更新时间索引设计：• PRIMARY KEY (id)• UNIQUE KEY uk_tag_name (name) — 标签名全局唯一，新增/编辑去重• INDEX idx_tag_group (group_name) — 按分组筛选（基础教育/大学考试/留学考试）• INDEX idx_tag_status (status) — 按启用/禁用状态筛选• INDEX idx_tag_sort (sort_order) — 按排序权重排序5.9 短语主表（phrase）短语主表存储 AI 识别出的短语和语法句型，一条短语一条记录。与单词共用标签体系，频次逻辑一致。字段名 | 字段类型 | 必填 | 默认值 | NULL | 说明id | bigint | 是 | 自增 | 否 | 主键自增phrase | varchar(100) | 是 | 无 | 否 | 短语文本，唯一索引，统一小写存储meanin